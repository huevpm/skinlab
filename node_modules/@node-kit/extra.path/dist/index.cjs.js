'use strict';

var path = require('path');

function dirname(path$1) {
  var pathArr = path$1.replace(new RegExp("".concat(path.sep, "?$")), "").split(path.sep);
  return pathArr.length > 0 ? pathArr.pop() : "";
}

exports.dirname = dirname;
