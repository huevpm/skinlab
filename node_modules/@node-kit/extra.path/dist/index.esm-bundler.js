import { sep } from 'path';

function dirname(path) {
  var pathArr = path.replace(new RegExp("".concat(sep, "?$")), "").split(sep);
  return pathArr.length > 0 ? pathArr.pop() : "";
}

export { dirname };
